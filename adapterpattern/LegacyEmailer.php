<?php
class LegacyEmailer {
    public function sendMail($to, $subject, $body) {
        // Simulating sending email (can replace with mail() or PHPMailer)
        echo "Email sent to: $to, Subject: $subject, Body: $body";
    }
}
?>
